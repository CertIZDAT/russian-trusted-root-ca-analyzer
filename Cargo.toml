[package]
name = "russia-ca-analyser"
version = "0.1.0"
edition = "2021"

[dependencies]
num_cpus = "1.15.0"
openssl = "0.10.50"
threadpool = "1.8.1"

[profile.release]
opt-level = "z"
codegen-units = 1
panic = "abort"
lto = true
